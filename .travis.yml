# Documentation: http://docs.travis-ci.com/user/languages/julia/
notifications:
  email: false

language: julia

os:
  - linux

julia:
  - 1.1
  - nightly

script:
  - julia --color=yes -e 'using Pkg; Pkg.test()'
